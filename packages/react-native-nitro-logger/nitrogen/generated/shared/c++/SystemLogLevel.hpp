///
/// SystemLogLevel.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrologger {

  /**
   * An enum which can be represented as a JavaScript union (SystemLogLevel).
   */
  enum class SystemLogLevel {
    LOG_TRACE      SWIFT_NAME(logTrace) = 0,
    LOG_DEBUG      SWIFT_NAME(logDebug) = 1,
    LOG_INFO      SWIFT_NAME(logInfo) = 2,
    LOG_WARN      SWIFT_NAME(logWarn) = 3,
    LOG_ERROR      SWIFT_NAME(logError) = 4,
    LOG_FATAL      SWIFT_NAME(logFatal) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrologger

namespace margelo::nitro {

  // C++ SystemLogLevel <> JS SystemLogLevel (union)
  template <>
  struct JSIConverter<margelo::nitro::nitrologger::SystemLogLevel> final {
    static inline margelo::nitro::nitrologger::SystemLogLevel fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("log-trace"): return margelo::nitro::nitrologger::SystemLogLevel::LOG_TRACE;
        case hashString("log-debug"): return margelo::nitro::nitrologger::SystemLogLevel::LOG_DEBUG;
        case hashString("log-info"): return margelo::nitro::nitrologger::SystemLogLevel::LOG_INFO;
        case hashString("log-warn"): return margelo::nitro::nitrologger::SystemLogLevel::LOG_WARN;
        case hashString("log-error"): return margelo::nitro::nitrologger::SystemLogLevel::LOG_ERROR;
        case hashString("log-fatal"): return margelo::nitro::nitrologger::SystemLogLevel::LOG_FATAL;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum SystemLogLevel - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrologger::SystemLogLevel arg) {
      switch (arg) {
        case margelo::nitro::nitrologger::SystemLogLevel::LOG_TRACE: return JSIConverter<std::string>::toJSI(runtime, "log-trace");
        case margelo::nitro::nitrologger::SystemLogLevel::LOG_DEBUG: return JSIConverter<std::string>::toJSI(runtime, "log-debug");
        case margelo::nitro::nitrologger::SystemLogLevel::LOG_INFO: return JSIConverter<std::string>::toJSI(runtime, "log-info");
        case margelo::nitro::nitrologger::SystemLogLevel::LOG_WARN: return JSIConverter<std::string>::toJSI(runtime, "log-warn");
        case margelo::nitro::nitrologger::SystemLogLevel::LOG_ERROR: return JSIConverter<std::string>::toJSI(runtime, "log-error");
        case margelo::nitro::nitrologger::SystemLogLevel::LOG_FATAL: return JSIConverter<std::string>::toJSI(runtime, "log-fatal");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert SystemLogLevel to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("log-trace"):
        case hashString("log-debug"):
        case hashString("log-info"):
        case hashString("log-warn"):
        case hashString("log-error"):
        case hashString("log-fatal"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
